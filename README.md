# Proyek Kriptografi 1

Kebutuhan:

- Go
- Compiler C (untuk Fyne)

Cara build aplikasi:

```sh
go build .
```

Cara menjalankan aplikasi:

```sh
go run .
```

> Tidak perlu build sebelum menjalankan, go akan otomatis rebuild jika perlu

